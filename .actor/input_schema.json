{
    "title": "AI Brand Sentinel (Lingo Powered)",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "comments": {
            "title": "Paste Global Feedbacks",
            "type": "string",
            "description": "Paste your customer reviews here. Separate each review with a comma (,).",
            "editor": "textarea",
            "prefill": "I love this product, Este producto es terrible, C'est correct"
        },
        "lingoApiKey": {
            "title": "Lingo API Key",
            "type": "string",
            "description": "Your API Key from Lingo.dev (Required for translation).",
            "editor": "textfield",
            "isSecret": true
        },
        "generateReply": {
            "title": "Auto-Generate Replies",
            "type": "boolean",
            "description": "Generate polite responses in the customer's native language.",
            "editor": "checkbox",
            "default": true
        }
    },
    "required": ["comments", "lingoApiKey"]
}